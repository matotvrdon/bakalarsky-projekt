services:
  caddy:
    image: caddy:2.8-alpine
    container_name: web-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      CADDY_EMAIL: ${CADDY_EMAIL}
      CADDY_DOMAIN_DEV: ${CADDY_DOMAIN_DEV}
      CADDY_DOMAIN_TEST: ${CADDY_DOMAIN_TEST}
      CADDY_DOMAIN_PROD: ${CADDY_DOMAIN_PROD}
    volumes:
      - ./docker/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - web-proxy

networks:
  web-proxy:
    name: web-proxy

volumes:
  caddy_data:
  caddy_config:
